require 'spec_helper'

describe Rubynception do
  it 'has a version number' do
    expect(Rubynception::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
